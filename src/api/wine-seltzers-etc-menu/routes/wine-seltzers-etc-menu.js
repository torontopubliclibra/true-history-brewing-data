'use strict';

/**
 * wine-seltzers-etc-menu router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::wine-seltzers-etc-menu.wine-seltzers-etc-menu');
