'use strict';

/**
 * wine-seltzers-etc-menu service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::wine-seltzers-etc-menu.wine-seltzers-etc-menu');
