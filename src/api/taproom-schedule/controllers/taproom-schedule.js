'use strict';

/**
 * taproom-schedule controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::taproom-schedule.taproom-schedule');
